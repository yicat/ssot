# docs/specs/

规格文档目录，**本项目的唯一事实源（SSOT）**。

## 命名与模板

`<feature>.spec.md`，固定五段：

1. 目的
2. 领域规则（Given-When-Then）
3. 验收标准
4. 边界与异常
5. 前后端契约

## 铁律

- 实现与规格冲突时，**先改规格，再改代码**
- 规格只描述**行为**，不写函数名、文件路径等实现细节
- 每条验收标准都要有对应的验收测试

## 动这一层之前

加载 `writing-spec` skill，它带了完整模板、五步流程与检查项。
