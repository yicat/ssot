# 设计复查：从原始诉求出发

> 目的：抛开既有路径，回到诉求本身，检查 984 行规格是否真的对准了要解决的问题。
> 本文是**自我批评**，不是总结。找的是站不住的地方，不是做得好的地方。

## 一、诉求清单（按提出顺序）

| # | 诉求 | 出处 |
|---|---|---|
| R1 | 从 wiki 文本记录最重要的游戏数据，**不能错** | 最早 |
| R2 | 软件提供数据库、schema、流程、校验方案 | 早期 |
| R3 | 管**数据、表、文档** | 早期 |
| R4 | 由唯一数据和文档生成**不同角度的方案/报告** | 早期 |
| R5 | 三个案例：每月攒资源 / 备战超鬼王 / 斗技阵容 | 中期 |
| R6 | **它不是死的，是有输入的**：个人式神池 + 偏好 | 中期 |
| R7 | 备选方案要多 | 近期 |
| R8 | 社区内容需**人工验证** | 近期 |

## 二、覆盖对账

| 诉求 | 覆盖 | 承载规格 | 问题 |
|---|---|---|---|
| R1 不能错 | ⚠️ **部分** | core / metamodel / admission / verification / ingestion | 治理手段齐全，但**"不能错"的最后一环是算对，而计算完全没做** |
| R2 数据库/schema/流程/校验 | ✅ | core / metamodel | 覆盖 |
| R3 数据、表、**文档** | ⚠️ | core | 文档只被定义为"有长文本字段的记录"，**它在系统中扮演什么角色没有定义** |
| R4 不同角度的方案 | ⚠️ | alternatives | 只定义了眼花缭乱时的多解与取舍，**没有定义"怎么算出一个方案"** |
| R5 三个案例 | ❌ | — | 只产出了 requires 清单，**一个都没跑通** |
| R6 个人输入 | ✅ | player-profile | 覆盖 |
| R7 多备选 | ✅ | alternatives | 覆盖 |
| R8 人工验证 | ✅ | verification | 覆盖，但**可行性存疑**（见下） |

## 三、五个站不住的地方

### 1（最严重）：核心诉求是"计算"，而计算是空白

你的原话：

> 阴阳师最重要的游戏机制是 pve 的伤害计算和速刷还有 pvp 的斗技，都需要**算伤害，速度，看御魂**

而实际发生的：

- 框架评估阶段我**发现**了 `text/template` 不能算术、离线无任何表达式库；
- 之后的五份规格里，我反复写「**不得使用任意表达式**」；
- 却**从未回答"那到底怎么算"**。

于是形成一个荒谬的结果：**我们建了一套精细的数据治理体系，来保证"数据不错"，但整个系统算不出一个伤害数字。**

而"不能错"的最后一环恰恰是**算对**。数据再准，公式算错，结果就是错的。

**这不是遗漏，是方向偏离。**

### 2：周期性 / 时间线完全没有建模

案例 1（每月攒勾玉/蓝票/黑蛋）的核心是**周期性**：每日任务、每周副本、每月活动、限时双倍。

我们的限定条件里有「版本 / 时间区间 / 场景 / 触发条件」，但**没有"每周期重复"**这个语义。
「每个月能攒多少」在当前模型里表达不出来。

案例 2 需要排期（距活动开始还剩多久、来不来得及），同样没有时间线模型。

### 3：文档的角色没定义

R3 明确要求管「文档」。我们的处理是：

- 定义了 `longtext` 类型
- 实测了中文检索（LIKE vs FTS5）
- 结论是"用 LIKE"

但**没有回答**：文档是**依据**（溯源锚点指向它）还是**内容**（用户要读的东西）？它如何参与方案？它本身要不要被校验？

我的判断是：**在这个系统里，wiki 原文的角色是「依据」**——429 个页面是断言的证据来源，不是产品内容。
这个定位必须**写下来**，否则实现时会走偏（比如去做一个 wiki 阅读器）。

### 4：核验层的精细度超过了执行能力

verification 有 107 行：四级方法、独立性判定、部分核验、抽检、重核验触发、人力排序。

但规模是：

- 275 个式神、上千个技能、1126 条 buff 条目
- **执行者可能只有你一个人**

一个人不可能核验完。**设计得再精细，没有执行者就是空的。**

我们确实写了"未核验允许使用但必须可见"，所以不会阻塞。但需要更明确的定位：
**默认全部不可信，按需核验，其余靠自动检查与社区共识。** 而不是设计一套假设有专职审核团队的流程。

### 5：大量能力与 wiki 自身重复

wiki **已经提供**：

| wiki 已有 | 我们重复造的部分 |
|---|---|
| 修订历史（`revid` + `parentid` + 时间戳） | provenance 的很大一块 |
| schema（`Data:Character.schema`，含类型与枚举） | 元模型的形状 |
| 多人编辑与回退 | 某种形式的人工审核 |

**我们的真实增量在 wiki 没有的地方**：

- 跨源交叉验证（灰机 vs biligame vs 社区）
- 断言分级（官方文案 vs 玩家补全，wiki 里是混在一起的）
- 系统化的冲突呈现（wiki 上两个页面矛盾，没人会系统性发现）
- **面向求解的投影**（伤害、配速、御魂）
- 与个人状态结合

重心应该压在**这些**上，而不是重新实现一套存储与版本管理。

## 四、两个真空白（必须补）

| 空白 | 影响 |
|---|---|
| **计算与公式**：怎么表达、怎么执行、怎么验证正确性 | 直接决定 R1/R4/R5 成立与否 |
| **时间与周期**：周期性产出、排期、时间线 | 直接决定 R5 的案例 1 与案例 2 |

## 五、复杂度是否超配

对比两条路：

**A（当前设计）**：断言模型 + 元模型 + 接入/准入 + 迁移 + 核验 + 方案 + 档案 ＝ 984 行规格，0 行实现

**B（极简）**：wiki JSON + 校验脚本 + Go 计算库 + 本地状态文件，用 git 管版本

B 能覆盖什么：

| 诉求 | B 能否覆盖 |
|---|---|
| 不能错 | ✅ 校验脚本 + 测试用例 |
| 跨源验证 | ✅ 脚本 |
| 溯源 | ✅ JSON 里记 revid |
| L1–L4 分级 | ✅ 就是一个字段 |
| 核验 | ✅ 一个 status 字段 + 审核界面 |
| 计算 | ✅ Go 代码（本来就要写） |
| 多解方案 | ✅ Go + 模板 |
| 迁移 | ✅ git diff + 迁移脚本 |

**A 的额外价值**在于：结构化的约束/冲突/时效管理，支撑**规模化与多写入方**。
**B 的优势**在于：单人单机、这个数据量级下，能**快得多**地跑出结果。

你的写入方是 agent / 脚本 / 外部同步（**多写入方**），所以准入与变更集**确实有价值**。
但现在的规模（423 页 / 275 式神）**还不需要**这层复杂度来兑现价值。

**结论不是推翻 A，而是：A 的价值要在规模出现后才兑现，现在应该先用最小可行路径跑通一个案例，再按需增厚。**

## 六、建议的调整

1. **立刻补「计算与周期」规格**——这是核心诉求，且是真空白。优先于任何实现。
2. **砍一个垂直切片先端到端跑通**，而不是全量实现。
   建议切片：**一个式神的属性 → 一次伤害计算 → 一个"我的式神够不够"的对比**。
   这条链同时穿过：接入 → 准入 → 断言 → 校验 → 计算 → 方案 → 个人状态。跑通了，架构就成立了；跑不通，问题会立刻暴露。
3. **核验层重新定位**为「标记为主、按需核验」，与单人执行能力匹配。
4. **写明文档的角色**：是依据，不是内容。
5. **列出"复用 wiki 已有能力"清单**，避免重复造。

## 七、如果重来，我会这样排序

```
1. 打通最短链路：一个页面 → 抽一条数据 → 校验 → 存 → 算一个数 → 输出
2. 补计算与周期（真空白，且是核心）
3. 扩数据面（经济/活动/版本）
4. 按需增厚治理层（迁移、核验精细化）
```

而不是：

```
1. 写 984 行规格（已完成）
2. …
```

**规格本身是资产，不是浪费**——它们把边界想清楚了。但**顺序错了**：
在验证最短链路能跑通之前，就先把治理层设计到了 984 行，
其中相当一部分能力（迁移引擎、核验流程）要等到规模出现才用得上。

## 八、一个需要你回答的问题

这套东西的**规模预期**是什么？

- 如果就是**你自己用**、数据量在千级、写入方其实主要是你手工触发的同步脚本：
  **B 路线的复杂度更匹配**，A 的很多层可以后置。
- 如果目标是**做成一个可给别人用的工具**、多写入方并发、长期维护：
  **A 的准入与变更集是必要的**，但也要接受"先跑通再增厚"。

这个答案会决定：下一步是做**计算与周期规格**，还是先**砍到最小可行路径**。

---

## 九、修正：在「做成通用工具、覆盖多场景」前提下的重新判断

规模预期确认后，上面若干结论需要改写。

### 被推翻的结论

**1. B 路线（极简）不成立。**

面对多场景、多写入方、长期演进，准入层与变更集**不是过度设计，是必要的结构**：

- 多场景意味着 schema 会持续演进 → 迁移机制必要
- 多写入方（agent / 脚本 / 同步）意味着并发与守门 → 准入必要
- 要给别人用意味着不可信输入增多 → 核验机制必要

### 被部分消解的结论

**2. 「核验人力不足」部分消解。**

原判断基于「执行者只有你一个人」。若做成工具：

> **工具提供核验的机制与工作台，核验由各场景的使用者自己做。**

你不需要核验所有人的数据——只需要核验你自己场景里你在意的那部分。
这反而让核验层的设计**更有意义**：它必须足够通用，能被不同场景的使用者使用。

### 被强化的结论

**3. 通用引擎的选择被印证。** 元模型必须封闭且领域无关——否则换个场景就要改代码。

**4. 「先跑通切片」比以前更重要。** 工具需要一个真实场景来证明通用性，而不是声称通用。

**5. 计算从「可绕过」升级为「必须解决」——这是最大的变化。**

之前的判断是：

> 若只有阴阳师，公式可以写死在 Go 代码里（`design-review.md` 第六节的替代方案）

**做成工具后，这条路被堵死**：不同场景的公式不同，你不能为每个场景改代码。

> **要做成覆盖多场景的工具，公式必须由使用者声明。**
> 因此必须有一个**表达式 / 公式引擎**——这是绕不过去的。

这同时说明：`alternatives.spec.md` 与 `metamodel.spec.md` 里反复出现的
「**不得使用任意表达式**」这条限制，**需要被重新设计**，而不是绕过。
当时的理由是「标准库无表达式求值、离线无表达式库」——那是**实现现状**，不是**设计目标**。

### 新增的架构轴：场景（Scenario）作为一等概念

工具与场景的职责必须切开：

| 层 | 工具提供 | 场景提供 |
|---|---|---|
| 存储、断言模型、准入、核验、迁移、投影引擎 | ✅ | |
| 元模型原语 | ✅（**封闭**） | |
| schema、校验规则、**计算规则**、视角定义 | 引擎 | **声明** |
| 数据源适配器、解析器 | 框架 | **实现或配置** |
| 核验 | 机制 + 工作台 | **人力** |
| 个人状态 | 机制 | **数据** |

- 场景之间数据隔离，元模型全局统一（保证引擎稳定）
- **场景被元模型卡住时**有两条出路：
  1. `opaque` 逃生舱——先记录，后建模
  2. **在 schema 层定义复合类型**——用现有原语的组合命名一个新类型。
     这是 schema 的能力，不是元模型的扩展，因此不破坏引擎稳定性

### 下一步优先级（改写后）

```
1. 计算引擎 + 时间模型   ← 工具的核心能力，也是最大空白
2. 场景包的概念          ← schema + 规则 + 计算 + 视角的可分发集合
3. 端到端切片            ← 用阴阳师作为第一个场景验证通用性
4. 其余治理层按需增厚
```

原来的第 1 条「计算与周期」不变，但**性质变了**：
它不再是「补一个阴阳师需要的功能」，而是**工具能不能成立的前提**。
